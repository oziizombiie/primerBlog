import React from "react" // get the React object from the react module

export default (props) => <h1 style={{textAlign:"center"}}>{props.message}</h1>  // expose the component to other modules // Always use props to receive data (it's only for lecture)